<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- This app's selected alarm is a user-facing core feature. Do not replace this
       with USE_EXACT_ALARM: Play reserves that normal permission for a narrow set
       of preinstalled alarm/calendar apps. -->
  <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />

  <application>
    <receiver
      android:name=".AffirmationAlarmReceiver"
      android:directBootAware="false"
      android:exported="false" />
    <receiver
      android:name=".AffirmationAlarmRestoreReceiver"
      android:directBootAware="false"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
        <action android:name="android.intent.action.TIMEZONE_CHANGED" />
        <action android:name="android.intent.action.TIME_SET" />
      </intent-filter>
    </receiver>
    <service
      android:name=".AffirmationPlaybackService"
      android:exported="false"
      android:foregroundServiceType="mediaPlayback" />
  </application>
</manifest>
